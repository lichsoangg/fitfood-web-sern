import React from 'react'

import MainLayout from './MainLayout'
export default {
  title: 'Layout/MainLayout',
  component: MainLayout
}

const Template = (args) => <MainLayout />

export const Main = Template.bind({})
